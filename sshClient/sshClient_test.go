package sshClient

import (
	"sftp/pwdfilgen"
	"testing"
)

func TestSSHSession(t *testing.T) {
	//set up uname,pwd as inp val
	pwdfilgen.DecodePwd(pwd)
	//set up a expected var as pointer to ssh session
	// call the actual func with inp val assert the return value

}
