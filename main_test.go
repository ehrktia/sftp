package main

import "testing"

func TestOperation(t *testing.T) {
	t.Run("check inp values",
		func(t *testing.T) {
			data := []string{"help"}
			if data[0]="help" {
				helpcontent.Helpcontent()
				
			}
		})

}
